package fundamentals2;

public class homework {
    public static void main(String[] args) {

       int y = 10;
       int x = 17;
       // if we didn't declare x, the code would not compile
        /*It's a local variable, so it needs to be initialised.
        * Local variables must be declared inside methods, constructors or blocks of code*/

       int result = x + y;
       System.out.println("the result is " + result);
    }
}
